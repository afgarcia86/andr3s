export function Logo() {
  return (
    <span className="inline-flex items-center font-mono font-bold text-xl tracking-tight select-none">
      andr<span className="text-accent">³</span>s
    </span>
  );
}
